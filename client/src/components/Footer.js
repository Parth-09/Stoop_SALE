import React from 'react';

const Footer = () => (
  <footer>
    <p>Share this event with your friends!</p>
    <button onClick={() => alert('Event shared!')}>Share</button>
  </footer>
);

export default Footer;
